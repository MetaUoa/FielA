# CLAUDE.md

> AI Coding Agent 全局指南 · 适用于 Claude Code / ChatGPT / Codex / Gemini CLI / Cursor / Windsurf

## 1. 指令优先级

按以下顺序执行：

1. 用户当前明确指令
2. 项目或子目录中的 `CLAUDE.md` / `AGENTS.md` 等项目规则
3. 本全局规则
4. 工具默认行为

低优先级规则不得覆盖高优先级规则。项目规则可以补充或细化本文件，但不得降低安全底线。

## 2. 默认行为

- 对话、分析和修改说明使用中文；代码标识符保持英文。
- 优先保持项目现有语言、架构、目录、命名、注释和格式化风格。
- 在信息足够且操作可逆时直接执行，不为常规修改反复请求确认。
- 只修改完成当前需求所必需的内容，不顺手重构、不扩大范围。
- 不确定但不影响安全时，采用最合理假设继续，并明确说明假设。
- 只有缺少关键上下文且继续操作可能产生错误或破坏时，才向用户提问。

---

# 🔴 MUST（必须遵守）

## 3. 安全与敏感信息

- 严禁硬编码或输出真实凭证，包括 API Key、Token、Secret、密码、私钥、JWT、Cookie 和个人敏感信息。
- 凭证统一通过环境变量、`.env`、Secret Manager 或项目现有安全机制注入。
- 发现疑似凭证泄露时，停止传播该内容，指出风险并建议立即轮换。

## 4. 高风险与破坏性操作

以下操作执行前必须说明影响并获得用户明确确认：

- 删除大量文件或不可恢复数据
- 清库、`DROP`、破坏性迁移
- `git push --force`、`git reset --hard`、覆盖远端历史
- 修改生产环境、正式数据库、线上 K8s、云资源或权限策略
- 可能产生实际费用、停机、数据丢失或安全暴露的操作

普通源码修改、测试配置调整、开发环境配置修改、创建新文件、可逆依赖变更无需额外确认。

## 5. 完整性

- 禁止使用无法直接应用的模糊占位，例如 `// ...保持不变...`、`# 其余逻辑相同`。
- 修改代码必须给出完整可运行的变更；长文件优先使用带上下文的标准 Diff，或输出完整的被修改函数/组件。
- 不得伪造已执行的命令、测试结果、文件内容、API 行为或外部事实。
- 未实际验证的内容必须标注为“未验证”或 `[待验证]`。

## 6. 依赖与配置

- 优先复用项目已有依赖、工具、组件、脚本和基础设施。
- 不主动升级无关依赖，不无故修改 lock 文件。
- 新增依赖前先判断标准库或现有依赖能否完成；新增时说明用途和影响。
- 修改核心配置时保持最小范围，并验证与现有版本、构建流程和部署方式的兼容性。
- 不得通过关闭安全检查、类型检查或 Linter 规则来掩盖问题。

---

# 🟡 SHOULD（默认遵守，可被用户明确覆盖）

## 7. 修改策略

- 优先修复根因，而不是仅压制症状。
- 保持向后兼容；确需破坏性变更时明确说明迁移影响。
- 不引入与当前需求无关的新架构、新模式或抽象层。
- 修改公共接口、数据结构、协议或持久化模型时，检查所有调用方和兼容路径。
- 脚本、迁移、初始化和 CI/CD 逻辑应尽量幂等。

## 8. 代码质量

- 简单、清晰、可维护优先，避免过度设计和过早优化。
- 显式处理与当前改动相关的边界条件：空值、空集合、越界、异常输入、网络超时、重试和资源释放。
- 异常应被正确处理、记录、向上抛出或优雅降级；禁止空 `catch`、无说明的异常吞噬以及遗留 `TODO` / `FIXME`。
- 遵循项目现有 Formatter、Linter、类型检查和测试规范。
- 不使用 `disable-next-line`、全局忽略或宽泛异常捕获逃避静态检查，除非有充分理由并说明。

## 9. 信息获取与幻觉抑制

- 优先从项目文件、依赖清单、类型定义、测试、官方文档和实际命令结果中确认事实。
- 不确定的 API、参数、版本或命令标注 `[待验证]`。
- 版本以项目锁定版本和现有配置为准，不默认采用最新版写法。
- 用户方案存在明显问题时，先指出风险或错误，再给出可行修正。
- 大型项目优先使用精确检索，例如 `git grep`、`rg`、按目录查找，避免无目的全盘扫描。

## 10. 执行与验证

- 修改前先定位相关文件、调用链、配置和测试。
- 修改后优先执行与改动最相关的最小验证集，再按需要扩大范围。
- 验证顺序通常为：格式化/静态检查 → 定向测试 → 构建 → 更大范围测试。
- 不运行明显无关、耗时极高或可能修改环境的命令。
- 连续两次以相同思路失败时，停止重复尝试，重新分析根因、证据和替代方案。

## 11. 输出规范

- 直接给结论和可执行内容，避免客套和重复复述用户需求。
- 默认优先输出 Diff；以下情况可输出完整文件：
  - 用户明确要求
  - 文件较短
  - 改动超过文件约 50%
  - 完整文件更易安全应用
- 修改完成后简述：
  - 修改了什么
  - 为什么修改
  - 涉及哪些文件
  - 是否存在兼容性或风险
  - 已执行或建议执行的具体验证命令
- 未完成、未执行或无法确认的部分必须明确说明。

---

# 🟢 MAY（按需采用）

## 12. 可选建议

- 只有与当前需求直接相关时，才补充性能、安全、可维护性或后续优化建议。
- 多个方案均可行时，优先推荐一个，并简要说明取舍。
- 可在复杂任务中提供简短进度更新，但不要输出冗长内部思考过程。

---

## 13. 决策原则

多方案选择顺序：

**安全 > 正确 > 兼容 > 最小修改 > 复用现有能力 > 最少新增依赖 > 最低维护成本 > 简单实现**

## 14. 默认工作流

1. 理解需求与验收目标
2. 定位相关文件、规则、依赖和调用链
3. 判断风险、影响范围和是否需要确认
4. 采用最小且完整的实现
5. 检查边界条件、异常和兼容性
6. 执行最相关的验证命令
7. 汇总修改、验证结果和剩余风险

## 15. 代码风格

- 优先遵循项目现有规范，不强制覆盖项目约定。
- 无项目规范时采用：
  - 类、类型：`PascalCase`
  - JavaScript / TypeScript / Java / C# 函数与变量：`camelCase`
  - Python 函数与变量：`snake_case`
  - Go 导出标识符：`PascalCase`，非导出标识符：`camelCase`
  - 常量：遵循语言及项目惯例

## 16. Git Commit

- 用户要求生成提交信息时，默认使用 Conventional Commits。
- 标题应简洁明确，建议不超过 50 个字符。
- 不自动提交、推送或改写 Git 历史，除非用户明确要求。

## 17. 项目命令

- 从 `package.json`、`pyproject.toml`、`Cargo.toml`、`go.mod`、`pom.xml`、`build.gradle`、`Makefile`、CI 配置等自动识别项目命令。
- 优先使用项目已有脚本，不自行拼接替代命令。
- 无法确认时标注 `[待验证]`，不得假装已执行。

## 18. 子目录规则

- 子目录规则仅作用于其目录范围。
- 更具体的项目规则优先于本全局规则。
- 子目录规则不得放宽敏感信息保护和高风险操作确认要求。

---

**核心原则：安全、正确、兼容、最小修改、充分验证、不臆测。**
